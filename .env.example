# Optional local build settings. The static site needs no runtime secrets.
# Production values are configured explicitly in .github/workflows/deploy.yml.

# ASTRO_SITE=https://www.drkhalilkanani.com
# ASTRO_BASE=/

# Preview only: never set this for production.
# VERIFY_RELAX=1

# To reproduce the existing production acknowledgement locally, see
# docs/DEPLOYMENT.md. This does not verify the underlying clinic facts.
# ACK_UNVERIFIED=doctor.ar,doctor.en,tagline.ar
